name: CI - Tests et Validation

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1. Récupération du repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Configuration de Python 3.12
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 3. Installation des dépendances complètes
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install rdflib pyshacl pytest faker anyio langsmith pydantic pydantic-settings

      # 4. Préparation de l'arborescence des dossiers de sortie
      - name: Ensure target directories exist
        run: |
          mkdir -p 02-Donnees/Snapshots_Phases/Phase1_Socle
          mkdir -p 02-Donnees/Snapshots_Phases/Phase2_ABox
          mkdir -p 02-Donnees/Master_Transversal/TLP_AMBER_Socle_TBox
          mkdir -p 02-Donnees/Master_Transversal/TLP_RED_Instances_ABox

      # 5. Exécution de la génération Phase 1
      - name: Run Phase 1 Generator
        run: |
          python3 03-Application/Phase1/generate_phase1_socle.py

      # 6. Exécution de la génération Phase 2
      - name: Run Phase 2 Generator
        run: |
          python3 03-Application/Phase2/generate_phase2_abox.py

      # 7. Exécution des tests Pytest
      - name: Run Pytest Suite
        env:
          PYTHONPATH: .
        run: |
          pytest -v 03-Application/Test/test_phase1_quality.py 03-Application/Test/test_phase2_abox.py --junitxml=test-results.xml

      # 8. Publication du rapport dans GitHub Actions
      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: "test-results.xml"

      # 9. Archivage des rapports
      - name: Upload Test Results Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-xml
          path: test-results.xml
